# Host-Rollen-Katalog — siehe docs/contracts/host-roles.md
- id: mssql_server
  label: "Microsoft SQL Server"
  category: database
  match:
    required_ports:
      - {port: 1433, proto: TCP}   # MS SQL Server (tds)
    any_ports: null
    optional_flags: []
  min_flows_per_port: 3
  base_confidence: 0.82            # Port 1433 ist nahezu eindeutig MS SQL Server
  confidence_bonus: {per_optional_port: 0.0, long_lived: 0.05}
  fingerprint: null
  mac_oui: []
