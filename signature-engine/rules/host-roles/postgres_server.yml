# Host-Rollen-Katalog — siehe docs/contracts/host-roles.md
- id: postgres_server
  label: "PostgreSQL Server"
  category: database
  match:
    required_ports:
      - {port: 5432, proto: TCP}   # PostgreSQL
    any_ports: null
    optional_flags: []
  min_flows_per_port: 3
  base_confidence: 0.82            # Port 5432 ist nahezu eindeutig PostgreSQL
  confidence_bonus: {per_optional_port: 0.0, long_lived: 0.05}
  fingerprint: null
  mac_oui: []
