# Host-Rollen-Katalog — siehe docs/contracts/host-roles.md
- id: ssh_host
  label: "SSH Host"
  category: remote-access
  match:
    required_ports:
      - {port: 22, proto: TCP}     # SSH
    any_ports: null
    optional_flags: []
  min_flows_per_port: 3
  base_confidence: 0.72            # 22/TCP weit verbreitet — schwaches Einzelsignal
  confidence_bonus: {per_optional_port: 0.0, long_lived: 0.05}
  fingerprint: null
  mac_oui: []
