# /run/cyjan-update als tmpfs-Verzeichnis bei jedem Boot anlegen.
# Wird vom tap-uplink-Container per Bind-Mount gemappt; das Verzeichnis
# muss dafür auf dem Host existieren bevor docker compose up läuft.
d /run/cyjan-update 0755 root root -
