# Cyjan IDS: Konfiguration ist in isolinux.cfg self-contained.
# Diese Datei existiert nur als Platzhalter für Legacy-INCLUDE-Referenzen
# und enthält bewusst keine LABELs oder TIMEOUTs (würden sonst mit
# isolinux.cfg kollidieren).
