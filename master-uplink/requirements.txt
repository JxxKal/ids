# aiohttp ersetzt die websockets-Lib für serverside Aufgaben — wir brauchen
# jetzt streaming-fähige HTTP-Responses für /tap-update/<file> (Bundles
# 300+ MB, das alte process_request lädt komplett in RAM und crashed).
# aiohttp.web.FileResponse nutzt sendfile, kein RAM-Buffer.
# WebSocket-Endpoint geht jetzt via aiohttp.web.WebSocketResponse — der
# tap-uplink-Client (websockets.connect) ist API-kompatibel mit aiohttp-Server.
aiohttp==3.9.5
asyncpg==0.29.0
confluent-kafka==2.3.0
orjson==3.9.15
cryptography>=42.0.0
# minio-py wird für PCAP-Upload aus tap-uplink-Frames in den ids-pcaps-Bucket
# verwendet. Tap-uplink schickt fertige PCAP-Bytes über mTLS-WSS, master-uplink
# legt sie unter alerts/<id>.pcap ab und setzt alerts.pcap_available=true.
minio==7.2.7
