websockets==12.0
confluent-kafka==2.3.0
orjson==3.9.15
httpx==0.27.0
python-jose[cryptography]>=3.3.0
qrcode==7.4.2
# Interner HTTP-Server auf :8090 (docs/internal-api.md). Gleiche Version wie
# master-uplink, das im Repo bereits einen aiohttp-Server betreibt.
aiohttp==3.9.5
# Config-Overlay aus system_config['app_connect'] — gleiche Version und
# gleiches Muster wie mqtt-bridge.
asyncpg==0.29.0
